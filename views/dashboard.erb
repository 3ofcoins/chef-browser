<div class="page-header">
  <h1>Last successful Chef run</h1>
</div>
<ul class="list-unstyled">
<% (nodes.sort_by {|node| [node[:automatic][:ohai_time] ? 1 : 0, node[:automatic][:ohai_time] ] }).each do |n| -%>
    <li>
      <a href="/node/<%= n.name %>"><%= n.name %></a>:
<%   if n[:automatic][:ohai_time] == nil -%>
    no recorded successful Chef run
<%   else -%>
<%= ((Time.now - Time.at(n[:automatic][:ohai_time])) / 3600).to_i.to_s %> hour(s)
<%= (((Time.now - Time.at(n[:automatic][:ohai_time])) / 60) % 60).to_i.to_s %> minute(s)
<%= ((Time.now - Time.at(n[:automatic][:ohai_time])) % 60).to_i.to_s %> second(s) ago
    </li>
<%   end -%>
<% end -%>
</ul>
