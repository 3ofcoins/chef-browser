<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= @title.join(' » ') %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/bootstrap-responsive.css" rel="stylesheet" media="screen">
    <link href="/css/bootstrap.css" rel="stylesheet" media="screen">
    <link href="/css/chef-browser.css" rel="stylesheet" media="screen">
    <style>
      body {
        padding-top: 80px;
      }
    </style>
  </head>
  <body>
    <div class="container-fluid" style="padding: 0 15px;">
        <div class="row-fluid">
          <div class="span12">
            <nav class="navbar navbar-fixed-top" role="navigation">
              <div class="navbar-inner">
                <a class="brand" href="/">Chef browser</a>
                <ul class="nav">
<% nav = ['node', 'environment', 'role', 'data bag']-%>
<% nav.each do |element| -%>
                  <li<%== [element, "#{element}s", "#{element} item", "#{element} items"].include?(resource_name) ? ' class="active"' : '' %>><a href="/<%= element.gsub(' ', '_') %>s"><%=  element.capitalize %>s</a></li>
<% end -%>
            </ul>
<% if settings.rb.node_search.length > 0 -%>
            <ul class="nav pull-right">
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Saved searches<b class="caret"></b></a>
                <ul class="dropdown-menu">
<%   settings.rb.node_search.each do |link_name, query| -%>
                  <li><a href='/nodes?q=<%= query %>'><%= link_name %></a></li>
<%   end -%>
                </ul>
              </li>
            </ul>
<% end -%>

<% if @search_url -%>
            <form class="navbar-search form-search pull-right" method="get" action="<%= @search_url %>">
              <div class="input-append">
                <input type="text" class="search-query input-small" name="q" placeholder="Search <%= @search_for %>"<% if params['q'] %> value="<%= params['q'] %>"<% end %>>
                <button type="submit" class="btn" id="search-submit"><i class="icon-search" title="search"></i></button>
              </div>
            </form>
<% end -%>
        </div>
      </nav>
    </div></div>
        <%== yield %>
    </div>
    <script src="/js/jquery-2.0.3.js"></script>
    <script src="/js/bootstrap.js"></script>
    <script src="/js/jquery.filtertable.js"></script>
    <script>
    $('table').filterTable({label: "", placeholder: "filter attributes", inputClass: "search-query"});
    </script>
  </body>
</html>
