<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Chef browser
      <%== resource_name == "data bags" || resource_name == "data bag items" ? " >> Data bags" : "" %>
      <%== resource_name == "data bag item" ? " >> Data bags >> #{data_bag}" : "" %>
      <%== resource_name == "role" || resource_name == "node" || resource_name == "environment" ? " >> #{resource_name.capitalize}s" : "" %>
      <%== resource_id == nil ? "" : " >> #{resource_id}" %>
      <%== search_query == nil ? "" : " >> Search results for #{search_query}" %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/bootstrap-responsive.css" rel="stylesheet" media="screen">
    <link href="/css/bootstrap.css" rel="stylesheet" media="screen">
    <link href="/css/chef-browser.css" rel="stylesheet" media="screen">
    <style>
      body {
        padding-top: 80px;
      }
    </style>
  </head>
  <body>
    <div class="container-fluid" style="padding: 0 15px;">
        <div class="row-fluid">
          <div class="span12">
            <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
              <div class="navbar-inner">
                <a class="brand" href="/">Chef browser</a>
                <ul class="nav">
<% nav = ['node', 'environment', 'role', 'data bag']-%>
<% nav.each do |element| -%>
                  <li<%== resource_name == element  || resource_name == "#{element}s" || resource_name == "#{element} item" || resource_name == "#{element} items" ? ' class="active"' : '' %>><a href="/<%= element.gsub(' ', '_') %>s"><%=  element.capitalize %>s</a></li>
<% end -%>
<% if settings.rb.node_search.length > 0 -%>
            </ul>
            <ul class="nav pull-right">
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Saved searches<b class="caret"></b></a>
                <ul class="dropdown-menu">
<%   settings.rb.node_search.each do |link_name, query| -%>
                  <li><a href='/nodes?q=<%= query %>'><%= link_name %></a></li>
<%   end -%>
                </ul>
              </li>
<% end -%>

        <%== yield %>
      </div>
    </div>
    <script src="/js/jquery-2.0.3.js"></script>
    <script src="/js/bootstrap.js"></script>
    <script src="/js/jquery.filtertable.js"></script>
    <script>
    $('table').filterTable({label: "", placeholder: "filter attributes", inputClass: "search-query"});
    </script>
  </body>
</html>
