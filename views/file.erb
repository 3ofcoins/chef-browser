<h1>
<% if file.type == 'recipes' -%>
<%= cookbook_name %>::<%= file.name.gsub('.rb', '') %>
<% else -%>
<%= file.path %>
<% end -%>
  <a role='button' class='btn btn-link' href='<%= url "/download#{request.path}" %>' title='Download file'><span class='glyphicon glyphicon-download'></span></a>
</h1>
<%== (ChefBrowser::FileContent.show_file(file, extname, content) || "<p class='lead'>The content of this file cannot be displayed. <a href='#{url "/download#{request.path}"}'>Download to your computer</a> to view it locally.</p>") %>
